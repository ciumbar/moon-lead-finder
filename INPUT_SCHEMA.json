{
  "title": "Moon Owner Leads ES Input",
  "description": "Busca anuncios públicos de habitaciones y pisos de particulares en Madrid y Barcelona.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "regions": {
      "title": "Regions",
      "description": "Ciudades o zonas objetivo.",
      "type": "array",
      "editor": "stringList",
      "default": ["Madrid", "Barcelona", "Comunidad de Madrid", "Área Metropolitana de Barcelona"]
    },
    "propertyTypes": {
      "title": "Property types",
      "description": "Tipos de inmueble a buscar.",
      "type": "array",
      "editor": "stringList",
      "default": ["room", "flat"]
    },
    "ownerOnly": {
      "title": "Owner only",
      "description": "Intentar guardar solo leads que parezcan particulares.",
      "type": "boolean",
      "default": true
    },
    "requireEmailOrPhone": {
      "title": "Require email or phone",
      "description": "Guardar solo resultados con email o teléfono.",
      "type": "boolean",
      "default": true
    },
    "requireSpanishPhone": {
      "title": "Require Spanish phone",
      "description": "Validar teléfonos con formato español cuando existan.",
      "type": "boolean",
      "default": true
    },
    "maxResultsPerQuery": {
      "title": "Max results per query",
      "description": "Número máximo de resultados a revisar por query de búsqueda.",
      "type": "integer",
      "minimum": 1,
      "default": 20
    },
    "maxPagesPerSite": {
      "title": "Max pages per site",
      "description": "Máximo de páginas internas a visitar por dominio.",
      "type": "integer",
      "minimum": 1,
      "default": 3
    },
    "allowedDomains": {
      "title": "Allowed domains",
      "description": "Lista opcional de dominios permitidos para restringir el rastreo.",
      "type": "array",
      "editor": "stringList",
      "default": []
    }
  },
  "required": ["regions"]
}
